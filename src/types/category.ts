export type CategoryProps = {
  _id: string;
  userId: string;
  title: string;
  Icon?: string; 
  color: string;
};

  export interface Category {
    _id?: string;
    userId: string;
    title: string;
    Icon?: string;
    color: string;
  }
  